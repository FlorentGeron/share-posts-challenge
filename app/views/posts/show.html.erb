

<div class= "container">
  <div class="page-wrapper">
    <%= link_to "Retour à la liste", posts_path%>
    <div class="post-details">
      <div class= post-view>
        <div class="post-title"><%=@post.title%></div>
        <div class= post-credits>
          <span>par <strong> <%=@post.user.nickname%> </strong> le <%=@post.created_at.strftime("%d.%m.%y")%></span>
          <%=link_to "Article Source", @post.url%>
        </div>
        <div class="post-content">
          <%=@post.content%>
        </div>
        <div class="comment-section">
          <%=@post.comments.count%> Commentaires :
          <%@post.comments.each do |comment|%>
            <div class="comment-preview">
              <strong><%=comment.user.nickname%></strong> a posté le <%=comment.created_at.strftime("%d.%m.%y")%>  :
              <%=comment.content%>
            </div>
          <%end%>
        </div>
        <div class= "new-comment">
          <%= simple_form_for [@post, @comment] do |f| %>
            <%= f.input :content, as: :text, label: "Ajouter votre Grain de Sel" %>
            <%= f.submit "Commenter", class: "button-large" %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
</div>
