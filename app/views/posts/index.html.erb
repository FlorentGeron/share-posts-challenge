
<div class= "container">
<h1>Liste des dernières publications</h1>
  <div class="posts-container">
    <% @posts.each do |post|%>
      <%= link_to post_path(post) do%>
        <div class= "post-preview">
          <div class="post-title"><%=post.title%></div>
          <div class= "post-credits">
            <span>Par <strong><%=post.user.nickname%></strong>
            le <%=post.created_at.strftime("%d %m %y")%></span>
            <%= post.comments.count%> commentaires
          </div>
        </div>
      <% end %>
    <%end%>
    </div>
</div>
